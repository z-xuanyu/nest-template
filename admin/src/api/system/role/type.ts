export interface addRoleForm {
  name: string;
  description: string;
  menuIds?: Array<string>;
}

export interface updateRoleForm {
  name: string;
  description: string;
  menuIds?: Array<string>;
}
